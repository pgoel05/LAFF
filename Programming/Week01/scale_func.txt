DESCRIPTION

The function that scales a vector x by a scalar alpha.

x must be either a nx1 array (column vector) or a 1xn array (row vector).

x_out must be the same kind of vector as x.

If x or alpha are not a (row or column) vector and scalar, respectively, the output should be 'FAILED'.